"""hhhelper — помощник кандидата для hh.ru.

Пакет содержит:
    * api      — клиент официального API hh.ru;
    * auth     — OAuth-авторизация и хранение токенов;
    * config   — конфигурация (фильтры, письма, настройки откликов);
    * filters  — движок фильтрации вакансий;
    * letters  — библиотека сопроводительных писем и их выбор;
    * storage  — история откликов (SQLite);
    * applier  — сценарий автоматических откликов;
    * cli      — интерфейс командной строки.
"""

__version__ = "1.0.0"
__all__ = ["__version__"]
